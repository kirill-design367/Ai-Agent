"use client";

import Hero3D from "@/components/home/Hero3D";

/*
  HERO — финальная спецификация (реф Active Theory / Lusion; вертикаль MONOLOG).
  Композиция сверху вниз:
  • верхняя панель — в SiteHeader (лого · текст-табы · «Обсудить проект»);
  • ЦЕНТР — манифест: крупная брендовая строка + мелкий подстрочник, максимум
    воздуха, увеличенный трекинг;
  • НИЗ — гигантская AUREA из частиц-точек край-в-край, срезана нижней кромкой
    экрана (низ букв уходит за край).

  КРИТИЧНО (баг возвращался 3 раза): H1 «Первое впечатление невозможно повторить»
  — ключевая брендовая фраза и SEO-заголовок. В РАЗМЕТКЕ обязаны быть пробелы
  между словами (обычный текстовый узел). Guard scripts/check-h1.mjs падает на
  сборке, если пробелы исчезнут. AUREA-частицы декоративны (aria-hidden) — бренд
  уже озвучен логотипом в шапке.
*/
function HeroInner() {
  return (
    <section className="hx hxf">
      <div className="hxf-manifest">
        <h1 className="hxf-lead">Первое впечатление невозможно повторить</h1>
        <p className="hxf-sub">
          Мы&nbsp;это знаем&nbsp;— поэтому каждый пиксель вашего сайта работает
          на&nbsp;доверие с&nbsp;первого взгляда.
        </p>
      </div>
      <Hero3D />
    </section>
  );
}

/* HeroV1 экспортируется как Suspense-fallback HomeLite. Разметка идентична
   основной (без клиентских хуков) → статический пререндер всегда содержит H1,
   LCP не блокируется канвасом, при гидратации картинка не «прыгает». */
export function HeroV1() {
  return <HeroInner />;
}

export default function Hero() {
  return <HeroInner />;
}
